from setuptools import setup

setup(
    name='cryptopals',
    version='0.1.0-dev',
    packages=['cryptopals'],
    install_requires=[
    ],
    tests_require=[
        'tox',
        'pytest',
        'pytest-cov',
    ],
)
