from setuptools import setup

setup(
    name='cryptopals',
    packages=['cryptopals'],
    install_requires=[
    ],
    tests_require=[
        'tox',
        'pytest',
        'pytest-cov',
    ],
)
